# Waypoint Viewer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build an interactive 3D web viewer for waypoints and edges in `path_config.yaml` — click waypoints to connect them, delete edges, filter by source/move_type.

**Architecture:** FastAPI backend (`robodk_code/waypoint_server.py`) serves `path_config.yaml` as JSON and handles edge writes. React + react-three-fiber frontend (`waypoint_viewer/`) renders the 3D scene. During dev: Vite dev server proxies `/api` to FastAPI. Production: FastAPI serves built frontend static files.

**Tech Stack:** Python 3.11 / FastAPI / uvicorn, Node.js / React 18 / Vite 5 / @react-three/fiber / @react-three/drei / three.js

---

## File Map

```
robodk_code/waypoint_server.py        — FastAPI app: GET waypoints/edges, POST/DELETE edge
tests/test_waypoint_server.py         — pytest: backend API tests

waypoint_viewer/
  package.json                        — npm deps + scripts
  vite.config.js                      — Vite config + /api proxy to localhost:8000
  index.html                          — HTML entry point
  src/
    main.jsx                          — React root mount
    App.jsx                           — data fetching, selection state, filter state
    Scene.jsx                         — Canvas + OrbitControls + lights
    Waypoint.jsx                      — sphere + orientation arrow per waypoint
    Edges.jsx                         — colored line per edge
    InfoPanel.jsx                     — sidebar: details, create/delete edge
    FilterBar.jsx                     — source + move_type checkboxes
    api.js                            — fetch helpers
```

**Coordinate mapping:** `path_config.yaml` uses robot coords (Z=up). Three.js uses Y=up. Throughout the frontend: `position = [wp.x, wp.z, wp.y]` (robot Z → Three.js Y).

---

## Task 1: FastAPI Backend

**Files:**
- Create: `robodk_code/waypoint_server.py`
- Create: `tests/test_waypoint_server.py`

- [ ] **Step 1: Install backend deps**

```bash
source /home/samst/miniconda3/etc/profile.d/conda.sh && conda activate robodk_v1
pip install fastapi "uvicorn[standard]" aiofiles httpx
```

Expected: no errors.

- [ ] **Step 2: Write the failing tests**

Create `tests/test_waypoint_server.py`:

```python
"""Tests for waypoint_server.py FastAPI backend."""
import pytest
import yaml
from fastapi.testclient import TestClient


SAMPLE_CONFIG = """\
waypoints:
  wp_a:
    x: 100.0
    y: 200.0
    z: 300.0
    rx: 0.0
    ry: 0.0
    rz: 90.0
    frame: robot_local
    move_type: MoveJ
    source: grasshopper
  wp_b:
    x: 400.0
    y: 0.0
    z: 500.0
    rx: 0.0
    ry: 0.0
    rz: 0.0
    frame: robot_local
    move_type: MoveL
    source: grasshopper
  home:
    joints: [0, 0, 0, 0, 0, 0, 0]
    source: human

# ── Edges (auto-generated by amalgamate_waypoints.py) ──────────────────
edges:
  - from: wp_a
    to:   wp_b
    tested: null
"""


@pytest.fixture
def config_file(tmp_path):
    p = tmp_path / "path_config.yaml"
    p.write_text(SAMPLE_CONFIG)
    return p


@pytest.fixture
def client(config_file, monkeypatch):
    import robodk_code.waypoint_server as srv
    monkeypatch.setattr(srv, "CONFIG_PATH", config_file)
    return TestClient(srv.app)


def test_get_waypoints_returns_list(client):
    r = client.get("/api/waypoints")
    assert r.status_code == 200
    data = r.json()
    assert isinstance(data, list)
    assert len(data) == 3  # wp_a, wp_b, home


def test_get_waypoints_includes_name(client):
    r = client.get("/api/waypoints")
    names = [w["name"] for w in r.json()]
    assert "wp_a" in names
    assert "wp_b" in names
    assert "home" in names


def test_get_edges_returns_list(client):
    r = client.get("/api/edges")
    assert r.status_code == 200
    data = r.json()
    assert len(data) == 1
    assert data[0]["from"] == "wp_a"
    assert data[0]["to"] == "wp_b"


def test_create_edge_adds_bidirectional(client, config_file):
    r = client.post("/api/edges", json={"from_name": "wp_b", "to_name": "home"})
    assert r.status_code == 200
    result = r.json()
    assert len(result["added"]) == 2

    edges = client.get("/api/edges").json()
    pairs = {(e["from"], e["to"]) for e in edges}
    assert ("wp_b", "home") in pairs
    assert ("home", "wp_b") in pairs


def test_create_edge_no_duplicate(client):
    # wp_a → wp_b already exists
    r = client.post("/api/edges", json={"from_name": "wp_a", "to_name": "wp_b"})
    assert r.status_code == 200
    result = r.json()
    # only wp_b → wp_a should be new
    assert len(result["added"]) == 1

    edges = client.get("/api/edges").json()
    a_to_b = [e for e in edges if e["from"] == "wp_a" and e["to"] == "wp_b"]
    assert len(a_to_b) == 1  # not duplicated


def test_delete_edge(client):
    r = client.request(
        "DELETE", "/api/edges",
        json={"from_name": "wp_a", "to_name": "wp_b"}
    )
    assert r.status_code == 200
    edges = client.get("/api/edges").json()
    assert not any(e["from"] == "wp_a" and e["to"] == "wp_b" for e in edges)
```

- [ ] **Step 3: Run tests to verify they fail**

```bash
source /home/samst/miniconda3/etc/profile.d/conda.sh && conda activate robodk_v1
cd /mnt/c/Users/samst/Framework/clones/custom_estimates_using_dhr_methods
pytest tests/test_waypoint_server.py -v
```

Expected: `ModuleNotFoundError: No module named 'robodk_code.waypoint_server'`

- [ ] **Step 4: Implement `robodk_code/waypoint_server.py`**

```python
"""
waypoint_server.py

FastAPI backend for the waypoint 3D viewer.

Run (WSL, robodk_v1 env):
    uvicorn robodk_code.waypoint_server:app --host 0.0.0.0 --port 8000 --reload

Open from Windows: http://localhost:8000  (WSL2 port-forwards automatically)
During dev, the Vite dev server runs separately on port 5173 and proxies /api to here.
"""
from pathlib import Path

import yaml
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from fastapi.staticfiles import StaticFiles
from pydantic import BaseModel

app = FastAPI(title="Waypoint Viewer")

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_methods=["*"],
    allow_headers=["*"],
)

REPO_ROOT = Path(__file__).parent.parent
CONFIG_PATH = REPO_ROOT / "robo_dk_output" / "path_config.yaml"
_EDGES_MARKER = "# ── Edges (auto-generated by amalgamate_waypoints.py) ──────────────────\n"


def _load() -> dict:
    with open(CONFIG_PATH, "r", encoding="utf-8") as f:
        return yaml.safe_load(f) or {}


def _read_text() -> str:
    with open(CONFIG_PATH, "r", encoding="utf-8") as f:
        return f.read()


def _write_edges(text: str, edges: list) -> None:
    lines = [_EDGES_MARKER + "edges:"]
    for e in edges:
        lines.append(f"  - from: {e['from']}")
        lines.append(f"    to:   {e['to']}")
        tested = e.get("tested")
        lines.append(f"    tested: {'null' if tested is None else tested}")
    block = "\n".join(lines) + "\n"
    idx = text.find(_EDGES_MARKER)
    text = (text[:idx] + block) if idx != -1 else (text.rstrip("\n") + "\n\n" + block)
    with open(CONFIG_PATH, "w", encoding="utf-8") as f:
        f.write(text)


@app.get("/api/waypoints")
def get_waypoints():
    raw = _load().get("waypoints") or {}
    return [{"name": k, **v} for k, v in raw.items() if isinstance(v, dict)]


@app.get("/api/edges")
def get_edges():
    return _load().get("edges") or []


class EdgeRequest(BaseModel):
    from_name: str
    to_name: str


@app.post("/api/edges")
def create_edge(req: EdgeRequest):
    text = _read_text()
    edges = list(_load().get("edges") or [])
    existing = {(e.get("from", ""), e.get("to", "")) for e in edges}
    added = []
    for frm, to in [(req.from_name, req.to_name), (req.to_name, req.from_name)]:
        if (frm, to) not in existing:
            edges.append({"from": frm, "to": to, "tested": None})
            added.append(f"{frm} -> {to}")
    _write_edges(text, edges)
    return {"added": added}


@app.delete("/api/edges")
def delete_edge(req: EdgeRequest):
    text = _read_text()
    edges = [
        e for e in (_load().get("edges") or [])
        if not (e.get("from") == req.from_name and e.get("to") == req.to_name)
    ]
    _write_edges(text, edges)
    return {"deleted": f"{req.from_name} -> {req.to_name}"}


# Serve built frontend in production
_dist = REPO_ROOT / "waypoint_viewer" / "dist"
if _dist.exists():
    app.mount("/", StaticFiles(directory=str(_dist), html=True), name="frontend")
```

- [ ] **Step 5: Run tests — expect all pass**

```bash
pytest tests/test_waypoint_server.py -v
```

Expected: 7 tests PASSED.

- [ ] **Step 6: Commit**

```bash
git add robodk_code/waypoint_server.py tests/test_waypoint_server.py
git commit -m "feat: FastAPI waypoint server with GET/POST/DELETE edges"
```

---

## Task 2: React App Scaffold

**Files:**
- Create: `waypoint_viewer/package.json`
- Create: `waypoint_viewer/vite.config.js`
- Create: `waypoint_viewer/index.html`
- Create: `waypoint_viewer/src/main.jsx`
- Create: `waypoint_viewer/src/api.js`

- [ ] **Step 1: Install Node.js in WSL (if not present)**

```bash
node --version 2>/dev/null || (
  curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash - &&
  sudo apt-get install -y nodejs
)
```

Expected: prints a version like `v20.x.x` or installs Node 20.

- [ ] **Step 2: Create `waypoint_viewer/package.json`**

```json
{
  "name": "waypoint-viewer",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "@react-three/drei": "^9.114.0",
    "@react-three/fiber": "^8.17.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  }
}
```

- [ ] **Step 3: Create `waypoint_viewer/vite.config.js`**

```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      '/api': 'http://localhost:8000'
    }
  }
})
```

- [ ] **Step 4: Create `waypoint_viewer/index.html`**

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Waypoint Viewer</title>
    <style>
      * { margin: 0; padding: 0; box-sizing: border-box; }
      body { overflow: hidden; }
    </style>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
```

- [ ] **Step 5: Create `waypoint_viewer/src/main.jsx`**

```jsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
```

- [ ] **Step 6: Create `waypoint_viewer/src/api.js`**

```js
const BASE = '/api'

export async function getWaypoints() {
  const r = await fetch(`${BASE}/waypoints`)
  if (!r.ok) throw new Error(`GET /api/waypoints failed: ${r.status}`)
  return r.json()
}

export async function getEdges() {
  const r = await fetch(`${BASE}/edges`)
  if (!r.ok) throw new Error(`GET /api/edges failed: ${r.status}`)
  return r.json()
}

export async function createEdge(from_name, to_name) {
  const r = await fetch(`${BASE}/edges`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ from_name, to_name }),
  })
  if (!r.ok) throw new Error(`POST /api/edges failed: ${r.status}`)
  return r.json()
}

export async function deleteEdge(from_name, to_name) {
  const r = await fetch(`${BASE}/edges`, {
    method: 'DELETE',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ from_name, to_name }),
  })
  if (!r.ok) throw new Error(`DELETE /api/edges failed: ${r.status}`)
  return r.json()
}
```

- [ ] **Step 7: Install npm deps**

```bash
cd /mnt/c/Users/samst/Framework/clones/custom_estimates_using_dhr_methods/waypoint_viewer
npm install
```

Expected: `node_modules/` created, no errors.

- [ ] **Step 8: Verify scaffold starts**

In one terminal (WSL, robodk_v1):
```bash
source /home/samst/miniconda3/etc/profile.d/conda.sh && conda activate robodk_v1
cd /mnt/c/Users/samst/Framework/clones/custom_estimates_using_dhr_methods
uvicorn robodk_code.waypoint_server:app --host 0.0.0.0 --port 8000 --reload
```

In another terminal (WSL):
```bash
cd /mnt/c/Users/samst/Framework/clones/custom_estimates_using_dhr_methods/waypoint_viewer
npm run dev
```

Open `http://localhost:5173` from Windows browser. Expected: blank page (no App yet), no console errors. Check `http://localhost:5173/api/waypoints` — expected: JSON array of waypoints.

- [ ] **Step 9: Commit**

```bash
cd /mnt/c/Users/samst/Framework/clones/custom_estimates_using_dhr_methods
git add waypoint_viewer/
git commit -m "feat: React + Vite scaffold for waypoint viewer"
```

---

## Task 3: 3D Scene — Waypoints and Edges

**Files:**
- Create: `waypoint_viewer/src/Scene.jsx`
- Create: `waypoint_viewer/src/Waypoint.jsx`
- Create: `waypoint_viewer/src/Edges.jsx`

- [ ] **Step 1: Create `waypoint_viewer/src/Waypoint.jsx`**

```jsx
import React, { useMemo } from 'react'
import * as THREE from 'three'
import { Line } from '@react-three/drei'

// Map robot move_type → sphere color (unselected)
const MOVE_COLORS = { MoveJ: '#4488ff', MoveL: '#44bb44' }

// Convert ZYX Euler (degrees) to the tool Z-axis direction in scene coords.
// Robot coord system: Z=up. Scene coord system: Y=up (we map robot z→scene y).
// Column 2 of R = Rz*Ry*Rx in robot space, then remap z→y.
function toolZAxis(rxDeg, ryDeg, rzDeg) {
  const toRad = (d) => (d * Math.PI) / 180
  const rx = toRad(rxDeg ?? 0)
  const ry = toRad(ryDeg ?? 0)
  const rz = toRad(rzDeg ?? 0)
  const cx = Math.cos(rx), sx = Math.sin(rx)
  const cy = Math.cos(ry), sy = Math.sin(ry)
  const cz = Math.cos(rz), sz = Math.sin(rz)
  // Column 2 (Z-axis of frame, robot space): [cx*cz*sy+sx*sz, cx*sy*sz-cz*sx, cx*cy]
  const robotX = cx * cz * sy + sx * sz
  const robotY = cx * sy * sz - cz * sx
  const robotZ = cx * cy
  // Map robot→scene: [x, z, y]
  return new THREE.Vector3(robotX, robotZ, robotY).normalize()
}

// robot coords → scene position [x, z, y]
function toScene(wp) {
  return [wp.x ?? 0, wp.z ?? 0, wp.y ?? 0]
}

const SPHERE_R = 25   // mm — visible at scene scale
const ARROW_LEN = 120 // mm

export default function Waypoint({ waypoint: wp, isSelected, onSelect }) {
  const pos = toScene(wp)
  const color = isSelected ? '#ffff00' : (MOVE_COLORS[wp.move_type] ?? '#aaaaaa')

  const arrowDir = useMemo(
    () => toolZAxis(wp.rx, wp.ry, wp.rz),
    [wp.rx, wp.ry, wp.rz]
  )

  const arrowEnd = [
    pos[0] + arrowDir.x * ARROW_LEN,
    pos[1] + arrowDir.y * ARROW_LEN,
    pos[2] + arrowDir.z * ARROW_LEN,
  ]

  return (
    <group>
      <mesh
        position={pos}
        onClick={(e) => { e.stopPropagation(); onSelect(wp.name) }}
      >
        <sphereGeometry args={[SPHERE_R, 16, 16]} />
        <meshStandardMaterial color={color} />
      </mesh>
      <Line points={[pos, arrowEnd]} color={color} lineWidth={2} />
    </group>
  )
}
```

- [ ] **Step 2: Create `waypoint_viewer/src/Edges.jsx`**

```jsx
import React from 'react'
import { Line } from '@react-three/drei'

// tested: null → grey, true → green, false → red
const COLORS = { true: '#44bb44', false: '#ff4444', null: '#555577' }

function toScene(wp) {
  return [wp.x ?? 0, wp.z ?? 0, wp.y ?? 0]
}

export default function Edges({ edges, waypointMap }) {
  return (
    <>
      {edges.map((edge, i) => {
        const from = waypointMap[edge.from]
        const to = waypointMap[edge.to]
        // Skip edges whose waypoints aren't in the current filtered view
        if (!from || !to || !('x' in from) || !('x' in to)) return null

        const testedKey = edge.tested === null ? 'null' : String(edge.tested)
        const color = COLORS[testedKey] ?? '#555577'

        return (
          <Line
            key={`${edge.from}__${edge.to}__${i}`}
            points={[toScene(from), toScene(to)]}
            color={color}
            lineWidth={1}
          />
        )
      })}
    </>
  )
}
```

- [ ] **Step 3: Create `waypoint_viewer/src/Scene.jsx`**

```jsx
import React, { useMemo } from 'react'
import { Canvas } from '@react-three/fiber'
import { OrbitControls } from '@react-three/drei'
import Waypoint from './Waypoint'
import Edges from './Edges'

export default function Scene({ waypoints, edges, selected, onSelect }) {
  const waypointMap = useMemo(() => {
    const m = {}
    waypoints.forEach((wp) => { m[wp.name] = wp })
    return m
  }, [waypoints])

  return (
    <Canvas
      camera={{ position: [4000, 2500, 3000], fov: 55, near: 1, far: 200000 }}
      style={{ width: '100%', height: '100%', background: '#1a1a2e' }}
      onPointerMissed={() => {}}
    >
      <ambientLight intensity={0.7} />
      <directionalLight position={[5000, 8000, 5000]} intensity={0.9} />
      <OrbitControls target={[1500, 800, 0]} makeDefault />

      {waypoints.map((wp) => (
        <Waypoint
          key={wp.name}
          waypoint={wp}
          isSelected={selected.includes(wp.name)}
          onSelect={onSelect}
        />
      ))}

      <Edges edges={edges} waypointMap={waypointMap} />
    </Canvas>
  )
}
```

- [ ] **Step 4: Create a minimal `waypoint_viewer/src/App.jsx` to render the scene**

```jsx
import React, { useState, useEffect, useCallback } from 'react'
import { getWaypoints, getEdges } from './api'
import Scene from './Scene'

export default function App() {
  const [waypoints, setWaypoints] = useState([])
  const [edges, setEdges] = useState([])
  const [selected, setSelected] = useState([])

  const reload = useCallback(async () => {
    const [wps, eds] = await Promise.all([getWaypoints(), getEdges()])
    setWaypoints(wps)
    setEdges(eds)
  }, [])

  useEffect(() => { reload() }, [reload])

  const visibleWaypoints = waypoints.filter(wp => 'x' in wp)

  const handleSelect = useCallback((name) => {
    setSelected(prev =>
      prev.includes(name) ? prev.filter(n => n !== name)
      : prev.length >= 2 ? [name]
      : [...prev, name]
    )
  }, [])

  return (
    <div style={{ width: '100vw', height: '100vh' }}>
      <Scene
        waypoints={visibleWaypoints}
        edges={edges}
        selected={selected}
        onSelect={handleSelect}
      />
    </div>
  )
}
```

- [ ] **Step 5: Verify in browser**

With both servers running (uvicorn + npm run dev), open `http://localhost:5173`.

Expected:
- Dark background 3D canvas
- Colored spheres at waypoint positions (blue=MoveJ, green=MoveL)
- Short colored lines showing tool Z-axis orientation from each sphere
- Grey/green/red lines between connected waypoints
- Orbit by dragging, zoom with scroll
- Clicking a sphere turns it yellow

- [ ] **Step 6: Commit**

```bash
git add waypoint_viewer/src/
git commit -m "feat: 3D scene with waypoint spheres, orientation arrows, and edge lines"
```

---

## Task 4: Info Panel + Edge Creation/Deletion

**Files:**
- Create: `waypoint_viewer/src/InfoPanel.jsx`

- [ ] **Step 1: Create `waypoint_viewer/src/InfoPanel.jsx`**

```jsx
import React from 'react'

const STATUS_COLORS = { true: '#44bb44', false: '#ff4444', null: '#888' }

function statusStr(tested) {
  return tested === null ? 'null' : String(tested)
}

export default function InfoPanel({ selected, edges, onCreateEdge, onDeleteEdge }) {
  // Edges where at least one endpoint is a selected waypoint
  const relevantEdges = edges.filter(e =>
    selected.some(wp => e.from === wp.name || e.to === wp.name)
  )

  return (
    <div style={{
      width: '300px',
      background: '#16213e',
      color: '#e0e0e0',
      padding: '16px',
      overflowY: 'auto',
      fontFamily: 'monospace',
      fontSize: '12px',
      flexShrink: 0,
    }}>
      <h3 style={{ margin: '0 0 12px', color: '#a0c4ff' }}>
        Selection ({selected.length}/2)
      </h3>

      {selected.length === 0 && (
        <p style={{ color: '#555' }}>Click waypoints to select. Select 2 to create an edge.</p>
      )}

      {selected.map(wp => (
        <div key={wp.name} style={{
          marginBottom: '10px', padding: '8px',
          background: '#0f3460', borderRadius: '4px',
        }}>
          <div style={{ fontWeight: 'bold', color: '#ffff00', marginBottom: '4px' }}>
            {wp.name}
          </div>
          {'x' in wp && (
            <div style={{ color: '#ccc' }}>
              <div>x: {wp.x?.toFixed(1)}  y: {wp.y?.toFixed(1)}  z: {wp.z?.toFixed(1)}</div>
              <div>rx: {wp.rx?.toFixed(1)}  ry: {wp.ry?.toFixed(1)}  rz: {wp.rz?.toFixed(1)}</div>
            </div>
          )}
          <div style={{ marginTop: '4px', color: '#aaa' }}>
            {wp.move_type && <span style={{ marginRight: '8px' }}>{wp.move_type}</span>}
            {wp.frame && <span style={{ marginRight: '8px' }}>{wp.frame}</span>}
            {wp.source && <span>{wp.source}</span>}
          </div>
          {wp.joints && (
            <div style={{ color: '#888', marginTop: '4px' }}>
              joints: [{wp.joints.map(j => j.toFixed(1)).join(', ')}]
            </div>
          )}
        </div>
      ))}

      {selected.length === 2 && (
        <button
          onClick={onCreateEdge}
          style={{
            width: '100%', padding: '10px', marginBottom: '16px',
            background: '#2a6a2a', color: '#7fff7f',
            border: '1px solid #44bb44', borderRadius: '4px',
            cursor: 'pointer', fontFamily: 'monospace', fontWeight: 'bold',
          }}
        >
          + Create Bidirectional Edge
        </button>
      )}

      {relevantEdges.length > 0 && (
        <>
          <h3 style={{ margin: '8px 0 8px', color: '#a0c4ff' }}>Connected Edges</h3>
          {relevantEdges.map((e, i) => {
            const s = statusStr(e.tested)
            return (
              <div key={i} style={{
                display: 'flex', alignItems: 'center',
                marginBottom: '4px', gap: '6px',
              }}>
                <span style={{
                  flex: 1, color: STATUS_COLORS[s] ?? '#888',
                  overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap',
                }}>
                  {e.from} → {e.to}
                </span>
                <span style={{ color: STATUS_COLORS[s] ?? '#888', fontSize: '10px', flexShrink: 0 }}>
                  {s}
                </span>
                <button
                  onClick={() => onDeleteEdge(e.from, e.to)}
                  style={{
                    background: '#4a1a1a', color: '#ff8888',
                    border: '1px solid #ff4444', borderRadius: '3px',
                    cursor: 'pointer', padding: '1px 6px', fontSize: '11px', flexShrink: 0,
                  }}
                >
                  ✕
                </button>
              </div>
            )
          })}
        </>
      )}
    </div>
  )
}
```

- [ ] **Step 2: Update `App.jsx` to wire InfoPanel and edge operations**

Replace the contents of `waypoint_viewer/src/App.jsx`:

```jsx
import React, { useState, useEffect, useCallback } from 'react'
import { getWaypoints, getEdges, createEdge, deleteEdge } from './api'
import Scene from './Scene'
import InfoPanel from './InfoPanel'
import FilterBar from './FilterBar'

export default function App() {
  const [waypoints, setWaypoints] = useState([])
  const [edges, setEdges] = useState([])
  const [selected, setSelected] = useState([])   // array of name strings, max 2
  const [filters, setFilters] = useState({ sources: null, moveTypes: null })

  const reload = useCallback(async () => {
    const [wps, eds] = await Promise.all([getWaypoints(), getEdges()])
    setWaypoints(wps)
    setEdges(eds)
  }, [])

  useEffect(() => { reload() }, [reload])

  const visibleWaypoints = waypoints.filter(wp => {
    if (!('x' in wp)) return false
    if (filters.sources && !filters.sources.includes(wp.source)) return false
    if (filters.moveTypes && !filters.moveTypes.includes(wp.move_type)) return false
    return true
  })

  const handleSelect = useCallback((name) => {
    setSelected(prev =>
      prev.includes(name) ? prev.filter(n => n !== name)
      : prev.length >= 2 ? [name]
      : [...prev, name]
    )
  }, [])

  const handleCreateEdge = useCallback(async () => {
    if (selected.length !== 2) return
    await createEdge(selected[0], selected[1])
    setSelected([])
    await reload()
  }, [selected, reload])

  const handleDeleteEdge = useCallback(async (fromName, toName) => {
    await deleteEdge(fromName, toName)
    await reload()
  }, [reload])

  const selectedWaypoints = selected
    .map(name => waypoints.find(w => w.name === name))
    .filter(Boolean)

  return (
    <div style={{ display: 'flex', flexDirection: 'column', height: '100vh' }}>
      <FilterBar waypoints={waypoints} filters={filters} onFiltersChange={setFilters} />
      <div style={{ display: 'flex', flex: 1, overflow: 'hidden' }}>
        <div style={{ flex: 1, position: 'relative' }}>
          <Scene
            waypoints={visibleWaypoints}
            edges={edges}
            selected={selected}
            onSelect={handleSelect}
          />
        </div>
        <InfoPanel
          selected={selectedWaypoints}
          edges={edges}
          onCreateEdge={handleCreateEdge}
          onDeleteEdge={handleDeleteEdge}
        />
      </div>
    </div>
  )
}
```

- [ ] **Step 3: Verify edge creation in browser**

1. Click one waypoint sphere → turns yellow, appears in panel
2. Click a second sphere → both yellow
3. Click "Create Bidirectional Edge" → panel updates, two new grey lines appear
4. Click ✕ on an edge → line disappears

- [ ] **Step 4: Commit**

```bash
git add waypoint_viewer/src/App.jsx waypoint_viewer/src/InfoPanel.jsx
git commit -m "feat: info panel with edge creation and deletion"
```

---

## Task 5: Filter Bar

**Files:**
- Create: `waypoint_viewer/src/FilterBar.jsx`

- [ ] **Step 1: Create `waypoint_viewer/src/FilterBar.jsx`**

```jsx
import React, { useMemo } from 'react'

export default function FilterBar({ waypoints, filters, onFiltersChange }) {
  const sources = useMemo(
    () => [...new Set(waypoints.map(w => w.source).filter(Boolean))].sort(),
    [waypoints]
  )
  const moveTypes = useMemo(
    () => [...new Set(waypoints.map(w => w.move_type).filter(Boolean))].sort(),
    [waypoints]
  )

  // null = show all; array = whitelist
  const isVisible = (key, value) =>
    filters[key] === null || filters[key].includes(value)

  const toggle = (key, value, allValues) => {
    onFiltersChange(prev => {
      const current = prev[key] === null ? allValues : [...prev[key]]
      const next = current.includes(value)
        ? current.filter(v => v !== value)
        : [...current, value]
      // If everything is checked, store null (no filter)
      return { ...prev, [key]: next.length === allValues.length ? null : next }
    })
  }

  const label = (checked, text) => (
    <label key={text} style={{
      cursor: 'pointer', marginLeft: '10px',
      opacity: checked ? 1 : 0.35,
      display: 'inline-flex', alignItems: 'center', gap: '4px',
    }}>
      <input
        type="checkbox"
        checked={checked}
        onChange={() => {}}  // handled by onClick on label
        style={{ cursor: 'pointer' }}
      />
      {text}
    </label>
  )

  return (
    <div style={{
      background: '#0f3460', padding: '6px 16px',
      display: 'flex', gap: '8px', alignItems: 'center',
      color: '#ccc', fontFamily: 'monospace', fontSize: '12px',
      flexShrink: 0, flexWrap: 'wrap',
    }}>
      <span style={{ color: '#a0c4ff', fontWeight: 'bold' }}>Source:</span>
      {sources.map(s => (
        <span key={s} onClick={() => toggle('sources', s, sources)} style={{ cursor: 'pointer' }}>
          {label(isVisible('sources', s), s)}
        </span>
      ))}

      <span style={{ borderLeft: '1px solid #333', marginLeft: '8px', paddingLeft: '16px', color: '#a0c4ff', fontWeight: 'bold' }}>
        Move type:
      </span>
      {moveTypes.map(m => (
        <span key={m} onClick={() => toggle('moveTypes', m, moveTypes)} style={{ cursor: 'pointer' }}>
          {label(isVisible('moveTypes', m), m)}
        </span>
      ))}

      <span style={{ marginLeft: 'auto', color: '#555' }}>
        {waypoints.filter(w => 'x' in w).length} waypoints · click spheres to select
      </span>
    </div>
  )
}
```

- [ ] **Step 2: Verify filters in browser**

1. Uncheck `grasshopper` → only human-sourced waypoints remain
2. Uncheck `MoveJ` → only MoveL spheres remain
3. Re-check all → everything visible again

- [ ] **Step 3: Commit**

```bash
git add waypoint_viewer/src/FilterBar.jsx
git commit -m "feat: filter bar for source and move_type"
```

---

## Task 6: Production Build + gitignore

**Files:**
- Modify: `.gitignore`

- [ ] **Step 1: Add node_modules and dist to .gitignore**

Add to `.gitignore` (append):
```
waypoint_viewer/node_modules/
waypoint_viewer/dist/
```

- [ ] **Step 2: Test production build**

```bash
cd /mnt/c/Users/samst/Framework/clones/custom_estimates_using_dhr_methods/waypoint_viewer
npm run build
```

Expected: `waypoint_viewer/dist/` created, no errors.

- [ ] **Step 3: Verify FastAPI serves the built frontend**

```bash
cd /mnt/c/Users/samst/Framework/clones/custom_estimates_using_dhr_methods
source /home/samst/miniconda3/etc/profile.d/conda.sh && conda activate robodk_v1
uvicorn robodk_code.waypoint_server:app --host 0.0.0.0 --port 8000
```

Open `http://localhost:8000` from Windows. Expected: full viewer loads from FastAPI alone (no npm dev server needed).

- [ ] **Step 4: Final commit and push**

```bash
cd /mnt/c/Users/samst/Framework/clones/custom_estimates_using_dhr_methods
git add .gitignore waypoint_viewer/
git commit -m "feat: complete waypoint 3D viewer — FastAPI + React/Three.js"
git push
```

---

## Running the Viewer

**Development (auto-reload on code changes):**

Terminal 1 — FastAPI backend:
```bash
source /home/samst/miniconda3/etc/profile.d/conda.sh && conda activate robodk_v1
cd /mnt/c/Users/samst/Framework/clones/custom_estimates_using_dhr_methods
uvicorn robodk_code.waypoint_server:app --host 0.0.0.0 --port 8000 --reload
```

Terminal 2 — Vite dev server:
```bash
cd /mnt/c/Users/samst/Framework/clones/custom_estimates_using_dhr_methods/waypoint_viewer
npm run dev
```

Open: `http://localhost:5173`

**Production (single server):**
```bash
cd waypoint_viewer && npm run build && cd ..
uvicorn robodk_code.waypoint_server:app --host 0.0.0.0 --port 8000
```

Open: `http://localhost:8000`
